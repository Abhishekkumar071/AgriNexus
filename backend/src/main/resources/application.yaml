spring:
  application:
    name: agrinexus-backend
  profiles:
    active: dev

server:
  port: 8080

app:
  cors:
    allowed-origins: http://localhost:5173

management:
  endpoint:
    health:
      probes:
        enabled: true
  endpoints:
    web:
      exposure:
        include: health,info